<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".activity.MainActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        app:liftOnScroll="true"
        app:liftOnScrollTargetViewId="@id/dsp_scrollview">

        <!--
            Keep the title toolbar and Settings action as sibling views. A nested ActionMenuView
            can be measured out of the visible action area on some Samsung/One UI layouts.
            The fixed 56dp end-aligned action target remains visible without affecting title centering.
        -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:titleCentered="true" />

            <androidx.appcompat.widget.ActionMenuView
                android:id="@+id/left_menu"
                android:layout_width="56dp"
                android:layout_height="match_parent"
                android:layout_gravity="end|center_vertical"
                android:importantForAccessibility="yes"
                android:minWidth="56dp"
                android:minHeight="48dp" />
        </FrameLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <include layout="@layout/content_main" />

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_gravity="bottom"
        android:contentInsetLeft="0dp"
        android:contentInsetStart="0dp"
        android:fitsSystemWindows="true"
        android:maxHeight="?attr/actionBarSize"
        app:contentInsetLeft="0dp"
        app:contentInsetStart="0dp"
        app:fabAlignmentMode="center"
        app:fabAnchorMode="cradle"
        app:menuAlignmentMode="auto" />

    <me.timschneeberger.rootlessjamesdsp.view.FloatingToggleButton
        android:id="@+id/power_toggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/power_button_alt"
        android:hapticFeedbackEnabled="true"
        app:layout_anchor="@id/bar"
        app:srcCompat="@drawable/ic_twotone_power_settings_24dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
